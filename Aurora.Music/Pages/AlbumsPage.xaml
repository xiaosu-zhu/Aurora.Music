<Page
    x:Class="Aurora.Music.Pages.AlbumsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aurora.Music.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Horizontal">
            <Grid Width="120" Height="120" Margin="32" HorizontalAlignment="Left"/>
            <StackPanel Margin="32" Orientation="Vertical" >
                <Grid Margin="8,4" Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock Text="Love" Style="{ThemeResource TitleTextBlockStyle}" Margin="8,4"/>
                </Grid>
                <Grid Margin="8,4" Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock Text="Beatles" Style="{ThemeResource BodyTextBlockStyle}" Margin="8,4"/>
                </Grid>
                <Grid Margin="8,4" Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock Text="13 Tracks" Style="{ThemeResource BodyTextBlockStyle}" Margin="8,4"/>
                </Grid>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
