<Page
    x:Class="Aurora.Music.Controls.LyricView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aurora.Music.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Loaded="Root_Loaded" x:Name="Root">
        <Grid x:Name="BackDrop" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        </Grid>
        <TextBlock Margin="0,0,0,96" Opacity="0.33333" Text="{x:Bind Lyric.GetPrevious(Lyric.CurrentIndex),Mode=OneWay}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}"/>
        <TextBlock Margin="0,96,0,0" Opacity="0.33333" Text="{x:Bind Lyric.GetNext(Lyric.CurrentIndex),Mode=OneWay}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}"/>

        <TextBlock Text="{x:Bind Lyric.GetCurrent(Lyric.CurrentIndex),Mode=OneWay}" MaxLines="1" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}" Margin="8"/>
    </Grid>
</Page>
